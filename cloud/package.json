{
  "name": "tokenizer-cloud",
  "version": "0.1.0",
  "description": "Tokenizer Cloud — backend API for session sync, analytics, and billing",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "db:migrate": "bun run src/db/migrate.ts"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "postgres": "^3.4.0",
    "jose": "^5.6.0",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
